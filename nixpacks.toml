[phases.setup]
nixPkgs = ["...", "jdk17"]

[phases.build]
cmds = [
  "cd Backend",
  "mvn clean package -DskipTests"
]

[phases.start]
cmd = "cd Backend && java -Dserver.port=$PORT -jar target/task-manager-1.0.0.jar"
